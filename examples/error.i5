#!/usr/bin/env i5

main = () => {
	result = divide(2, 0)
	if err ? result {
		throw $error.new("error: " : err.message)
	}
	$print(result)
	# output: "error: can not divide by zero!"
}

divide = (a b) => {
	if b == 0 {
		return $error.new("can not divide by zero!")
	}
	return a / b
}
