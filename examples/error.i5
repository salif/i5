#!/usr/bin/env i5

fn main() {
	result = divide(2, 0)
	if err ? result {
		throw err
	}
	$print(result)
	# output: "error: can not divide by zero!"
}

fn divide(a b) {
	if b == 0 {
		return $error.new("can not divide by zero!")
	}
	return a / b
}
